
export const coursesReducer = (state =  {}, action) => {
    switch (action.type) {
        case "SOME_COURSE_CASE": {
            return state;
        }
        default:
            return state;
    }
}